## 修复方案

### 问题原因
`updateLoginFailCount` 方法有多个参数，但 XML 中 parameterType 设置不正确，导致 MyBatis 无法正确映射参数。

### 解决方案
1. **修改 UserMapper 接口**：使用 `@Param` 注解明确指定参数名称
2. **修改 UserMapper.xml**：移除错误的 parameterType，使用 @Param 指定的参数名