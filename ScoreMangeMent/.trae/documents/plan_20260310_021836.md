## 移除登录失败抖动动画

### 修改内容：
1. **删除 shake 状态变量** - 不再需要控制抖动
2. **移除抖动 class 绑定** - 从模板中删除 `:class="{ 'shake': shake }"`
3. **删除抖动相关代码** - 移除 `showError` 函数中的抖动逻辑
4. **删除 CSS 动画样式** - 移除 `.shake` 和 `@keyframes shake` 样式