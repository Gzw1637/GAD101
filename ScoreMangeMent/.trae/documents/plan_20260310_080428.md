## 修改计划

### 需求说明

1. **用户管理（AdminUser.vue）**
   - 移除"添加用户"按钮
   - 移除"删除"按钮
   - 只保留"编辑"功能

2. **学生管理（AdminStudent.vue + StudentServiceImpl.java）**
   - 完善删除功能，同时删除 `zw_student` 表和 `g_user` 表的记录
   - 使用事务保证数据一致性

### 具体修改内容

#### 1. AdminUser.vue - 移除添加和删除功能

**移除添加按钮：**
```vue
<template #header>
  <div class="card-header">
    <span>用户管理</span>
    <!-- 移除添加用户按钮 -->
  </div>
</template>
```

**移除删除按钮：**
```vue
<el-table-column label="操作" width="150" fixed="right">
  <template #default="scope">
    <el-button type="primary" size="small" @click="handleEditUser(scope.row)">编辑</el-button>
    <!-- 移除删除按钮 -->
  </template>
</el-table-column>
```

**移除相关方法：**
- 删除 `handleAddUser` 方法
- 删除 `handleDeleteUser` 方法
- 删除 `dialogType` 为 'add' 的相关逻辑
- 修改对话框标题固定为"编辑用户"

#### 2. StudentMapper.xml - 添加删除用户的方法

```xml
<delete id="deleteUserByUserId" parameterType="java.lang.Long">
    DELETE FROM g_user WHERE USER_ID = #{userId}
</delete>
```

#### 3. StudentMapper.java - 添加删除用户的方法

```java
int deleteUserByUserId(Long userId);
```

#### 4. StudentServiceImpl.java - 完善删除功能

```java
@Override
@Transactional
public int deleteStudent(Long studentId) {
    // 先查询学生信息获取 userId
    StudentVO student = studentMapper.selectById(studentId);
    if (student == null) {
        throw new RuntimeException("学生不存在");
    }
    
    // 先删除学生表记录
    int studentResult = studentMapper.deleteById(studentId);
    if (studentResult <= 0) {
        throw new RuntimeException("删除学生失败");
    }
    
    // 再删除用户表记录
    int userResult = studentMapper.deleteUserByUserId(student.getUserId());
    if (userResult <= 0) {
        throw new RuntimeException("删除用户失败");
    }
    
    return studentResult;
}
```

### 涉及文件
- `AdminUser.vue` - 移除添加和删除功能
- `StudentMapper.xml` - 添加删除用户 SQL
- `StudentMapper.java` - 添加删除用户方法
- `StudentServiceImpl.java` - 完善删除逻辑

### 预期效果
- **用户管理**：只能编辑用户信息，无法添加和删除用户
- **学生管理**：删除学生时，同时删除关联的用户记录，保持数据一致性