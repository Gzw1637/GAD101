## 问题分析

### 问题 1：考试代码不显示
从截图看，数据库中的 `EXAM_CODE` 值为 `20190201`（bigint 类型），但前端表格显示为空。

**原因**：数据库中的 `EXAM_CODE` 是 `bigint(20)` 类型，值为 `20190201`，但前端可能没有正确接收到这个值。

### 问题 2：更新失败
**原因**：前端在编辑时，`examId` 字段可能没有正确填充。需要检查前端是否正确获取到 `examId`。

### 解决方案

1. **在 ExamVO 中添加 toString 方法**，方便调试查看数据
2. **在前端添加调试日志**，查看接收到的数据
3. **确保前端正确处理 examId**
4. **添加考试代码的显示格式化**（如果需要）

### 修改文件
- `ScoreBackend/src/main/java/org/gzw/backend/entity/vo/ExamVO.java` - 添加 toString 方法
- `ScoreFrontend/src/views/AdminExam.vue` - 添加调试日志，确保数据正确处理

### 调试步骤
1. 添加 toString 方法后重启后端
2. 打开浏览器控制台查看接收到的数据
3. 确认 examId 是否正确传递

是否确认开始修改？