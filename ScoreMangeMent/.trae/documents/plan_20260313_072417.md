## 修复 Excel 模板格式问题

### 问题
当前模板的表头是竖向排列的（A 列），不符合常规的 Excel 使用习惯，用户填写数据不方便。

### 解决方案
修改 StudentController.java 的模板生成逻辑：
- 删除示例数据行（第 11 行的数据）
- 只保留第一行的表头
- 使用 `head` 参数传入表头定义，让 EasyExcel 自动生成横向表头
- 不写入数据行，用户下载后从第二行开始填写

### 修改内容
修改 `downloadTemplate` 方法，调整表头生成方式。