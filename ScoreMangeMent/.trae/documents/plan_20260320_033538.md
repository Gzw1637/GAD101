# 修复 application.yml 配置文件

## 问题分析
配置文件中有两个 `spring:` 配置块：
- 第 4 行：第一个 `spring:` 配置块
- 第 24 行：第二个 `spring:` 配置块（导致重复键错误）

## 修复方案
删除第 24 行的重复 `spring:` 键，将 multipart 配置正确缩进并合并到第一个 spring 配置块中。

## 修改内容
保持第 4-14 行的 spring 配置不变，将第 24-28 行的 multipart 配置合并到第 14 行之后，作为 spring 的下一级配置。