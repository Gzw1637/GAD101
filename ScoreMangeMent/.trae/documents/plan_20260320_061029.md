# 修复编辑对话框显示数字编码问题

## 问题原因
后端返回的字段值是数字类型（如 `10141001`），但前端 `el-option` 的 `value` 是字符串格式（如 `"10141001"`），类型不匹配导致 Element Plus 无法正确匹配并显示标签。

## 解决方案
修改 `handleEditAdvice` 方法，在填充表单数据时将数字转换为字符串：
- `adviceType.toString()`
- `senderType.toString()`
- `adviceStatus.toString()`