## 修复计划

### 问题定位
后端接口工作正常，已返回 12 条成绩数据。问题出在前端 `StudentScore.vue` 第 185 行的日期排序逻辑，JavaScript 无法正确解析后端返回的 LocalDateTime 格式。

### 需要完成的任务

1. **修复日期排序逻辑**
   - 在 `loadScoreList` 方法中，添加对 examTime 的容错处理
   - 使用更可靠的日期比较方式，处理可能的空值或格式问题

2. **添加调试日志**
   - 在获取数据后打印 response.data，方便查看实际返回的数据格式

### 修改文件
- `ScoreFrontend/src/views/StudentScore.vue` - 修复 loadScoreList 方法中的日期排序逻辑

### 技术要点
- 使用 `Date.parse()` 或添加格式转换
- 添加空值检查，避免 examTime 为 null 时出错
- 保持代码简洁，不影响其他功能