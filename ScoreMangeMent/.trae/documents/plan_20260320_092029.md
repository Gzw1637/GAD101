## 修改 AdminScore 页面 - 模糊查询 + 科目选择

### 前端修改 ([AdminScore.vue](file://c:\Users\26868\Desktop\ScoreMangeMent\ScoreFrontend\src\views\AdminScore.vue))

**1. 修改搜索栏（第 16-26 行）**
- 将 `el-select` 考试下拉框改为 `el-input` 输入框
- 添加科目选择下拉框
- 修改变量名：`selectedExamId` → `filterExamName`

**2. 修改 script 部分**
- 添加 `filterExamName` 和 `filterSubjectType` 变量
- 修改 `loadScoreList` 方法，支持按考试名称模糊查询和科目筛选
- 添加 `handleSearch` 搜索方法
- 修改 `resetExamFilter` 方法为 `resetSearch`
- 添加科目选项数组（类似 StudentScore.vue）

**3. 后端保持不变**
- 使用已有的 `/api/score/list` 接口，传递 `examName` 和 `subjectType` 参数
- 后端 Service 层会处理模糊查询逻辑

### 修改要点
1. 考试名称输入框支持回车搜索
2. 科目下拉框支持清空选择
3. 搜索和重置功能
4. 前端分页改为后端分页（因为需要传递筛选条件到后端）