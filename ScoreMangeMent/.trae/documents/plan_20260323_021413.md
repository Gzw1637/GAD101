**问题分析**
提交成功了，但是内容显示为 `[object Object]`，这表明 `adviceDesc` 仍然是一个对象，而不是字符串。当 `adviceDesc` 是一个对象时，`String(adviceDesc)` 会返回 `"[object Object]"`，这就是为什么我们看到的是 `[object Object]`。

**解决方案**
修改 `handleSubmit` 函数，确保 `adviceDesc` 总是一个字符串，并且正确处理 Delta 对象的情况。

**修改的文件**
- `ScoreFrontend/src/views/StudentAdviceSent.vue`

**具体实现**
1. 重写 `handleSubmit` 函数中的富文本内容处理逻辑
2. 确保无论 `adviceDesc` 是什么类型，最终都会转换为字符串
3. 特别处理 Delta 对象的情况，提取其中的纯文本内容

**修改步骤**
1. 打开 `StudentAdviceSent.vue` 文件
2. 修改 `handleSubmit` 函数，重写富文本内容处理逻辑
3. 确保 `adviceDesc` 总是一个字符串，并且正确处理 Delta 对象的情况

这样提交的内容就会正确显示，而不是显示为 `[object Object]`。